import { ListNode } from "../../lib/list-node.ts";

/**
 * Given the head of a linked list, return true if the list contains a cycle.
 * Return false if walking `next` pointers eventually reaches null.
 *
 * Example:
 * head = 3 -> 2 -> 0 -> -4, with -4 pointing back to 2
 * returns true
 */
export function hasCycle(head: ListNode | null): boolean {
}
