import { ListNode } from "../../lib/list-node.ts";

/**
 * Given a linked list `L0 -> L1 -> ... -> Ln`, reorder it in place to
 * `L0 -> Ln -> L1 -> Ln-1 -> L2 -> ...`.
 *
 * Do not return a new list; mutate the existing nodes.
 */
export function reorderList(head: ListNode | null): void {
}
