/**
 * Given a string containing only `()[]{}`, return true if every opening bracket
 * is closed by the same type of bracket in the correct order.
 *
 * Example:
 * s = "()[]{}"
 * returns true
 *
 * s = "(]"
 * returns false
 */
export function isValidParentheses(s: string): boolean {
}
