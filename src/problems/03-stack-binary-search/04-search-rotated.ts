/**
 * Given a sorted array that may have been rotated at an unknown pivot, return
 * the index of `target`.
 *
 * Return -1 if the target is not present.
 *
 * Example:
 * nums = [4, 5, 6, 7, 0, 1, 2], target = 0
 * returns 4
 */
export function searchRotated(nums: number[], target: number): number {
}
