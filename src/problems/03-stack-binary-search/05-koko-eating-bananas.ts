/**
 * Koko has `piles` of bananas and `h` hours. At speed `k`, she eats up to `k`
 * bananas from one pile per hour.
 *
 * Return the minimum integer speed `k` that lets her finish all piles within
 * `h` hours.
 */
export function minEatingSpeed(piles: number[], h: number): number {
}
