/**
 * Given a sorted integer array `nums` and a `target`, return the index of
 * `target` if it exists. Return -1 otherwise.
 *
 * Example:
 * nums = [-1, 0, 3, 5, 9, 12], target = 9
 * returns 4
 */
export function binarySearch(nums: number[], target: number): number {
}
