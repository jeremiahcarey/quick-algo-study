/**
 * Given non-negative values in `nums`, return the maximum amount you can rob
 * without robbing two adjacent houses.
 *
 * Example:
 * nums = [2, 7, 9, 3, 1]
 * returns 12 by robbing 2, 9, and 1
 */
export function rob(nums: number[]): number {
}
