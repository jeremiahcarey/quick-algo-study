/**
 * You can climb either 1 or 2 steps at a time. Given `n` steps, return the
 * number of distinct ways to reach the top.
 *
 * Example:
 * n = 3
 * returns 3: 1+1+1, 1+2, or 2+1
 */
export function climbStairs(n: number): number {
}
