/**
 * Given an array of unique integers, return all possible subsets.
 *
 * The result should include the empty subset and the full array. The order of
 * subsets does not matter.
 *
 * Example:
 * nums = [1, 2, 3]
 * returns [[], [1], [2], [3], [1, 2], [1, 3], [2, 3], [1, 2, 3]]
 */
export function subsets(nums: number[]): number[][] {
}
