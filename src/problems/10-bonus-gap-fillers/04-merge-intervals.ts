/**
 * Given an array of intervals where each interval is `[start, end]`, merge all
 * overlapping intervals and return the non-overlapping result.
 *
 * Intervals that touch at an endpoint count as overlapping.
 *
 * Example:
 * intervals = [[1, 3], [2, 6], [8, 10], [15, 18]]
 * returns [[1, 6], [8, 10], [15, 18]]
 */
export function mergeIntervals(intervals: number[][]): number[][] {
}
