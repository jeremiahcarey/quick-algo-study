/**
 * Given an integer array `nums`, where each value is the maximum jump length
 * from that index, return true if you can reach the last index starting from
 * index 0.
 *
 * Example:
 * nums = [2, 3, 1, 1, 4]
 * returns true because you can jump from index 0 to 1, then to the last index
 */
export function canJump(nums: number[]): boolean {
}
