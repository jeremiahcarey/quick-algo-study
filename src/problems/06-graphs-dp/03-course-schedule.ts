/**
 * Given `numCourses` and prerequisite pairs `[course, prerequisite]`, return
 * true if it is possible to finish all courses.
 *
 * Return false when prerequisite dependencies contain a cycle.
 *
 * Example:
 * numCourses = 2, prerequisites = [[1, 0]]
 * returns true because course 0 can be taken before course 1
 *
 * numCourses = 2, prerequisites = [[1, 0], [0, 1]]
 * returns false because the courses depend on each other
 */
export function canFinish(numCourses: number, prerequisites: number[][]): boolean {
}
