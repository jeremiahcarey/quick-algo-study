import { GraphNode } from "../../lib/graph-node.ts";

/**
 * Given a reference to a node in a connected undirected graph, return a deep
 * clone of the graph.
 *
 * Each cloned node should have the same value and neighbor structure, but must
 * be a different object from the original node.
 *
 * Example:
 * adjacency = [[2, 4], [1, 3], [2, 4], [1, 3]]
 * returns a new graph with the same adjacency structure
 */
export function cloneGraph(node: GraphNode | null): GraphNode | null {
}
