import { TreeNode } from "../../lib/tree-node.ts";

/**
 * Given the root of a binary search tree and two existing nodes `p` and `q`,
 * return their lowest common ancestor.
 *
 * The lowest common ancestor is the deepest node that has both `p` and `q` as
 * descendants. A node can be a descendant of itself.
 */
export function lowestCommonAncestor(root: TreeNode | null, p: TreeNode, q: TreeNode): TreeNode | null {
}
