import { TreeNode } from "../../lib/tree-node.ts";

/**
 * Given the root of a binary tree, invert it by swapping every node's left and
 * right children. Return the root of the inverted tree.
 *
 * Example:
 * root = [4, 2, 7, 1, 3, 6, 9]
 * returns [4, 7, 2, 9, 6, 3, 1]
 */
export function invertTree(root: TreeNode | null): TreeNode | null {
}
