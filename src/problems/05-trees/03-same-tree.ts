import { TreeNode } from "../../lib/tree-node.ts";

/**
 * Given two binary tree roots, return true if the trees have the same shape and
 * the same values at every corresponding node.
 */
export function isSameTree(p: TreeNode | null, q: TreeNode | null): boolean {
}
