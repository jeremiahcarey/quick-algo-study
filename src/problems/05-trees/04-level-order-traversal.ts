import { TreeNode } from "../../lib/tree-node.ts";

/**
 * Given the root of a binary tree, return its values level by level from top to
 * bottom, left to right within each level.
 */
export function levelOrder(root: TreeNode | null): number[][] {
}
