import { TreeNode } from "../../lib/tree-node.ts";

/**
 * Given the root of a binary tree, return true if it is a valid binary search
 * tree.
 *
 * Every node in a left subtree must be strictly less than the node, and every
 * node in a right subtree must be strictly greater.
 *
 * Example:
 * root = [2, 1, 3]
 * returns true
 *
 * root = [5, 1, 4, null, null, 3, 6]
 * returns false
 */
export function isValidBST(root: TreeNode | null): boolean {
}
