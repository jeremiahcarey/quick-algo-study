/**
 * You are given box types where each type is `[numberOfBoxes, unitsPerBox]`,
 * plus `truckSize`, the maximum number of boxes the truck can carry.
 *
 * Choose boxes to maximize the total number of units on the truck.
 *
 * Example:
 * boxTypes = [[1, 3], [2, 2], [3, 1]], truckSize = 4
 * returns 8 by taking 1 box worth 3 units, 2 boxes worth 2 units each, and
 * 1 box worth 1 unit
 */
export function maximumUnits(boxTypes: number[][], truckSize: number): number {
}
