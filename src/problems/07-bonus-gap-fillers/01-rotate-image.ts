/**
 * Given an n x n 2D matrix representing an image, rotate it 90 degrees
 * clockwise in place.
 *
 * Do not return a new matrix. Mutate the input matrix directly.
 *
 * Example:
 * matrix = [
 *   [1, 2, 3],
 *   [4, 5, 6],
 *   [7, 8, 9],
 * ]
 * after calling rotateImage(matrix), matrix is [
 *   [7, 4, 1],
 *   [8, 5, 2],
 *   [9, 6, 3],
 * ]
 */
export function rotateImage(matrix: number[][]): void {
}
