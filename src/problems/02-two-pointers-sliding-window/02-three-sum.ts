/**
 * Given an integer array `nums`, return all unique triplets `[a, b, c]` such
 * that `a + b + c === 0`.
 *
 * Do not return duplicate triplets. The order of triplets does not matter.
 */
export function threeSum(nums: number[]): number[][] {
}
