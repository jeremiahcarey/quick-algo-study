/**
 * Given an array of integers `nums` and an integer `target`, return the indexes
 * of two different elements whose values add up to `target`.
 *
 * Assume exactly one valid answer exists. Return the two indexes in any order.
 *
 * Example:
 * nums = [2, 7, 11, 15], target = 9
 * returns [0, 1] because nums[0] + nums[1] === 9
 */
export function twoSum(nums: number[], target: number): number[] {
}
