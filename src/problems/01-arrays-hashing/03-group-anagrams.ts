/**
 * Given an array of strings, group together words that are anagrams of each
 * other. Each word should appear in exactly one group.
 *
 * Treat each array element as one complete string. Do not split strings into
 * sentences or words.
 *
 * The order of groups and the order of words inside each group do not matter.
 *
 * Example:
 * strs = ["eat", "tea", "tan", "ate", "nat", "bat"]
 * returns [["eat", "tea", "ate"], ["tan", "nat"], ["bat"]]
 */
export function groupAnagrams(strs: string[]): string[][] {
}
